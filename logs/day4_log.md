# Day 4 Log
- 今日目标：跑通 Baseline 和 RAG 应用对比，产出量化数据。

- 完成情况：已完成。历经基础设施崩溃与重建，最终成功产出真实对比数据。

- 关键产出：
  1. reports/eval_results_day4.csv (包含 q001 到 q006 的 Baseline 和 RAG 完整对比数据)。
  2. reports/failure_analysis_day4.md (涵盖业务幻觉、检索漂移、基础设施崩溃与数据重建等5大类核心问题记录)。
  3. 完成知识库底层基础设施架构升级（从 OpenAI 系模型全面迁移至硅基流动开源模型）。

- 验证结果（数据对比与表现）：
  1. 引用准确率：Baseline 0%，RAG 实现有效引用100%。
  2. 业务幻觉测试 (q001, q002)：Baseline 严重跑偏，输出“分布式训练”、“宏观产业链”等脱离产品经理设定的废话；RAG 精准引用官方 JD 的“Builder能力”、“Agent实操”等要求。
  3. 防幻觉极限测试 (q006)：Baseline 试图编造“火星AI产品经理”的岗位职责；RAG 成功触发兜底规则，明确拒答。
  4. 简历改写测试 (q005)：RAG 精准提炼 Moonshot AI 官方的“AI重构运营”、“Builder能力”并针对性改写 Bullet，表现尚可。

- 卡点与排查过程（超出计划之外的配置问题）：
  1. API 额度耗尽导致全链路瘫痪：Dify 平台自带的 200 条免费额度耗尽，导致 LLM、Embedding、Rerank 链路全面崩溃。
  2. 向量空间漂移与数据重建失败：切换至硅基流动的 BAAI/bge-m3 模型后，由于新旧模型向量维度不兼容且系统未自动触发重算，导致文档显示“可用”但检索结果为 0。
  3. 解决方案：果断执行“物理删库重建”策略。删除旧文档，重新上传 `jd_clean.csv`，强制系统用新模型重新计算全部向量空间。同时调整了召回设置（Top K=5, Score阈值=0.35），彻底恢复了 RAG 的检索能力。

- 明日依赖：Day 5 需定义并实现 3 个工具（Tool Use），开始 Agent 动手能力的实战。

- 面试可讲点（核心话术准备）：
  1. **基础设施容灾与数据重建**：我经历了 RAG 系统的“生死时刻”，深刻掌握了“更换 Embedding 模型必须伴随全量数据重算”的底层工程逻辑。我能在系统全面瘫痪（额度耗尽、数据失效）时，果断执行降级策略（切换开源模型）并重建数据，证明了我极强的线上问题排查与容灾能力。
  2. **用数据说话**：通过严谨的 A/B 测试，量化了 RAG 相对于纯 LLM 在“幻觉控制”和“业务贴合度”上的绝对优势（引用准确率从 0 提升到 100% ，中等难度的问题回答质量有待提高）。
  3. **产品判断力与事实求是**：在排查 q005 失败原因时，我避免了使用“高级词汇（如语义错位）”来掩盖真实的技术故障，而是坚持查底层的报错日志，最终定位到是“API额度耗尽导致的数据索引未更新”。在还没有完全摸清AI运作底层机制的情况下（即还未熟练到可以描绘其整体架构和工作流图景），这体现了作为 AI 产品经理的排查直觉。